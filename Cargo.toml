[package]
name = "chatty-factory-rebuild"
version = "0.1.0"
edition = "2021"
license = "AGPL-3.0-only"

[lib]
name = "chatty_factory_rebuild"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
tempfile = "3"

[dev-dependencies]
trybuild = "1"
